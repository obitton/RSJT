# REV02 handoff: prompt for the next session (2026-09-23)

Superseded on 2026-09-24: wave 1 was dispatched, reviewed, and pushed. Do not paste this prompt again. The current state is in REV02-handoff-2026-09-24-cloud.md.

Paste everything below the rule into a fresh session opened in C:\dev\RSJT. It is written for a mid-tier model acting as dispatcher and reviewer, not as an executor.

---

You are the dispatcher and reviewer for REV02, a remediation plan for the RSJT repo at C:\dev\RSJT. You do not edit application code yourself. Your job is to dispatch five implementation subagents for wave 1, review what they produce against the plan, and report verdicts to Ofir. Read this whole message before running anything.

## 1. Summary of current work

### What has been done

1. ✅ A code-quality audit of RSJT (2026-09-08) found: two safety bugs in apps/api/src/config.ts (the boolean env flags are parsed with z.coerce.boolean, so the string "false" becomes true, and the messaging recipient allowlist is parsed but never enforced); a user-visible label bug for canceled jobs in two mobile format files; drizzle migration snapshots missing for migrations 0002 to 0011; about 2,500 lines of hand-written mobile parsers duplicating shared zod schemas; boilerplate duplication in screens, repositories, and route tests; no CI.
2. ✅ The fix plan was written and reviewed three times: .project-management/4-revisions/REV02-audit-remediation.md (untracked, not yet committed). Section 0 is the executor contract, 1 is preflight, 2 is the wave table, 3 holds slices 0 to 7, 4 lists deferred items, 5 is your reviewer checklist, 6 is the dispatch prompt.
3. ✅ Preflight is green on this machine as of 2026-09-23 and recorded in the plan's section 1 block: Node v24.13.1, pnpm 9.15.4, install, lint, typecheck, mobile lint, Metro export, migrate, seed, and the full test baseline (52 files, 311 tests) all exit 0. Docker container rsjt-postgres-1 holds rsjt_dev plus the per-slice databases rsjt_slice1, rsjt_slice2, rsjt_slice3, rsjt_slice5, each migrated and seeded.
4. ✅ Process decisions already made: executors work in their own git worktrees (C:\dev\rsjt-sliceN) with their own databases; they commit on local branches only; nobody pushes, opens PRs, or merges until Ofir says so.

### What is left

1. Dispatch wave 1: slices 0, 1, 2, 3, 5, in parallel.
2. Review each slice's report and diff against section 5 of the plan.
3. Report per-slice verdicts to Ofir and stop. Wave 2 (slices 4 and 7) and wave 3 (slice 6) run only after Ofir merges wave 1.

## 2. Instructions

### Context

The plan is the single source of truth. When this message and the plan disagree, the plan wins, and you tell Ofir about the disagreement. Everything you run goes through Git Bash, because the plan's commands use `VAR=value command` prefixes and process substitution.

### Before dispatch

1. In Git Bash: `cd /c/dev/RSJT && git status --porcelain && git worktree list`. Status must show only the two untracked files under .project-management/4-revisions (the plan and this handoff). The worktree list must show only the main checkout. If rsjt-slice worktrees already exist, a previous session dispatched; stop and ask Ofir.
2. `docker ps --filter "publish=54329" --format "{{.Names}} {{.Status}}"` must print rsjt-postgres-1 Up. If Docker is not running, start Docker Desktop, wait for `docker info` to succeed, then run `pnpm db:up`.
3. `docker exec rsjt-postgres-1 psql -U rsjt -d postgres -tAc "SELECT datname FROM pg_database WHERE datname LIKE 'rsjt%' ORDER BY 1"` must list rsjt_dev, rsjt_slice1, rsjt_slice2, rsjt_slice3, rsjt_slice5. Recreate any missing one with the loop in plan section 1 step 4 (only the missing N). A duplicate-key error from seed on an already-seeded database means "already seeded" and is fine.
4. Sanity, not a full preflight: `pnpm lint` and `pnpm typecheck` from the root, both exit 0. If either fails, stop and report; do not fix anything.
5. Read the plan: sections 0, 1, 2, 5, and 6 in full. Skim the section 3 headings. Read a slice in full only when you review it.

### Dispatch

Launch five subagents in one message, in the background, one per slice, using the strongest implementation model available to you (Opus tier). Each prompt is plan section 6 verbatim with N and <name> substituted from this table, followed by the filled-in preflight results block copied from plan section 1.

| N | name |
|---|---|
| 0 | ci-workflow |
| 1 | config-safety |
| 2 | canceled-label |
| 3 | migration-snapshots |
| 5 | api-dedup |

Do not paraphrase the dispatch prompt and do not add instructions beyond the substitutions. Wait for the reports. Do not start reviewing a slice before its report arrives, and never do an executor's work for it.

### Review, per slice, in this order (plan section 5)

1. The report must match the template in plan section 0.6 with an exit code on every verification line. If it does not, send the same subagent one message: "Fill the template in section 0.6; missing: <lines>." Nothing else.
2. In C:\dev\rsjt-sliceN: `git diff --stat main...rev02-sliceN-<name>` must list only files the slice's "Files you may change" allows.
3. In the same worktree: `git diff --name-only main...HEAD | xargs grep -n $'\u2014'` must be empty, and `git diff --name-only main...HEAD | xargs grep -nE "TODO|FIXME|as unknown as|: any\b|<any>|@ts-(ignore|expect-error)|biome-ignore|eslint-disable"` must be empty, except that slice 1 may show the pre-existing line at twilio-outbound-messenger.ts:61.
4. Read the diff against the slice's numbered steps. Anything not in the steps must appear under "Deviations from the plan" with a reason you accept; otherwise the slice is rejected.
5. Re-run the slice's Verify commands yourself inside its worktree with `DATABASE_URL=postgres://rsjt:rsjt_local@localhost:54329/rsjt_sliceN` exported on every command that touches Postgres. Record the exit codes in your verdict.
6. Predicted behavior changes for wave 1, and the only ones allowed: slice 1, the three enabled messenger tests gain an allowlist entry and sends to non-allowlisted recipients are blocked; slice 5, job update feed and lead conversion responses carry cancelReason and canceledAt for canceled jobs. Any other behavior change is a rejection reason.
7. Rejected slice: send the executor one message listing exactly what to change and which plan rule it broke, wait, and re-review from step 1. After two rejections of the same slice, stop and report to Ofir.

### Report to Ofir, then stop

One table with columns: slice, branch, worktree, verdict (accept, rework, blocked), diff stat, your verify exit codes, deviations. Below it, one question: merge wave 1 into local main and push? Then end your turn. Do not merge, push, open PRs, delete worktrees, remove databases, or start wave 2.

### Hard rules

- Git Bash for every command. An exported DATABASE_URL beats the value in .env.
- Never run `biome check --write` or `pnpm lint:fix` across the repo. Never edit .agents, skills-lock.json, pnpm-lock.yaml, or migration SQL yourself.
- No em dashes in anything you write, including messages to executors and the final report. No Linear, Notion, Slack, Gmail, Drive, or Tenex systems. No live Twilio, RepairShopr, or AI calls.
- The plan file is untracked; executors read it from C:\dev\RSJT, not from their worktree. The dispatch prompt already says so.
- If a stated fact in the plan turns out to be wrong, stop and tell Ofir which one. Do not patch the plan silently.
- Memory notes for this project are in C:\Users\Ofir\.claude\projects\C--dev-RSJT\memory.

## 3. Key files

| File | Role |
|---|---|
| .project-management/4-revisions/REV02-audit-remediation.md | The plan. Untracked. Sections 0, 1, 2, 5, 6 are yours; section 3 is per slice. |
| .project-management/4-revisions/REV02-handoff-2026-09-23.md | This handoff. |
| AGENTS.md | Project constraints: no Tenex tooling, no live customer-facing writes, no em dashes. Its "Allowed Working Area" names a macOS path; on this machine the repo is C:\dev\RSJT and slice 7 fixes the wording later. |
| .env | Created from .env.example on 2026-09-23, gitignored. Tests do not load it. The api dev script would crash on its empty values until slice 1 lands; that is expected and not your problem. |
| apps/api/src/config.ts, apps/api/src/integrations/twilio/twilio-outbound-messenger.ts | Slice 1 targets. |
| apps/mobile/src/dashboard/dashboard-format.ts, reminders/reminder-format.ts, jobs/fact-format.ts | Slice 2 targets. |
| packages/db/src/migrations/meta/, packages/db/src/migrate.ts | Slice 3 targets. Slice 3 also creates and drops two scratch databases and a scratch folder; its report must say they are gone. |
| apps/api/src/repositories/, apps/api/src/tests/ | Slice 5 targets. |
| .github/workflows/ci.yml | Slice 0 creates it. It cannot run until a branch is pushed; that check is deferred by design. |

Constraints: preflight is done, do not repeat it beyond the sanity checks above. Do not create databases for slices 4, 6, or 7 yet. Do not commit the plan or this handoff; Ofir decides that.
