# IP01.11 - Manager Dashboard - Implementation Plan Review

**Date:** 2026-05-26
**Plan Reviewed:** `.project-management/3-implementation-plans/01-service-referral-ops-agent/IP01.11-manager-dashboard.md`
**Local Ticket:** RSJT-011

## Rubric Scorecard

| Dimension | Score (0-10) | Notes |
| --- | ---: | --- |
| Non-breaking / Stability | 9.2 | The plan adds read-only manager routes and mobile views on top of existing data. It avoids external sends and RepairShopr writes. |
| Correctness | 9.1 | The proposed `/manager/dashboard` and `/manager/jobs/:jobId` routes align with existing Fastify auth guards, route wiring, and current Expo Router group structure. |
| Simplicity / Elegance | 9.0 | A thin service plus repository keeps aggregation local and avoids premature state libraries or complex mobile routing. |
| Completeness | 9.2 | The plan covers shared DTOs, API repository/service/routes, mobile client, validators, UI states, tests, and local ticket cleanup. |
| Convention Alignment | 9.4 | It uses local project-management files, `manager` role wording, shared Zod contracts, TanStack Query, and existing mobile component patterns. |

**Readiness Score:** 91.9%

**Reasoning:** The plan is ready for implementation. The only meaningful risk is query complexity in the dashboard repository, which is contained by DB-backed grouping tests.

## Critical Issues

None. The plan is ready for implementation.

## Suggested Improvements

1. **`apps/api/src/repositories/manager-dashboard-repository.ts`:** Keep aggregation queries readable even if that means a few focused queries instead of one dense join. This will make the first dashboard implementation easier to verify.
2. **`apps/mobile/src/app/(shared)/jobs/[jobId].tsx`:** Keep the non-manager fallback simple until tech-specific job detail work exists. Avoid building a second detail model prematurely.
3. **Dashboard DTOs:** Include `updatedAt` in dashboard rows so the UI can sort and display recency without local inference.

## Review Summary

**Grade:** 91.9% (Fully ready)

**Critical Issues:** 0 blocking issues found
**Suggested Improvements:** 3 non-blocking improvements suggested

**Recommendation:** Ready for implementation. Suggested improvements can be applied during execution.

**Next Steps:**

1. Execute `IP01.11` before tickets that depend on takeover dashboard visibility.
2. Run the full verification block before marking RSJT-011 done.
