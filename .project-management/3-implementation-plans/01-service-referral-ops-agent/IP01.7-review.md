# IP01.7 - Tech Update Extraction Service - Implementation Plan Review

**Date:** 2026-05-22
**Plan Reviewed:** `IP01.7-tech-update-extraction-service.md`
**Reviewer:** Local implementation-plan reviewer

## Rubric Scorecard


| Dimension                | Score | Notes                                                                                                                                                                                             |
| ------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Non-breaking / Stability | 9.4   | Adds new shared contracts, repositories, service, and routes without changing existing auth, matching, or RepairShopr behavior. External writes are explicitly out of scope.                      |
| Correctness              | 9.2   | Aligns with existing `messages`, `extracted_facts`, auth, route validation, and Fastify dependency injection patterns. The confidence basis-point storage plan matches existing repository style. |
| Simplicity / Elegance    | 8.9   | The adapter boundary is useful without adding a live provider prematurely. Repository responsibilities are narrow and testable.                                                                   |
| Completeness             | 9.3   | Covers shared schemas, persistence, route wiring, service behavior, representative extraction rules, and deterministic tests.                                                                     |
| Convention Alignment     | 9.5   | Preserves `manager` and `tech` role vocabulary, local-only project management, Zod contracts, Fastify routes, Drizzle repositories, and fixture-backed tests.                                     |


**Readiness Score:** 92.7%

**Reasoning:** The plan is implementation-ready. It creates the extraction slice as a local, testable backend capability while keeping live AI, messaging sends, RepairShopr writes, Linear, and Tenex systems out of scope.

## Critical Issues

None. The plan is ready for implementation.

## Suggested Improvements

1. `**apps/api/src/services/update-extraction-service.ts`**: Keep the deterministic adapter private to the API package for now. Export only the interface and service unless tests need direct adapter construction.
2. `**apps/api/src/tests/updates-routes.test.ts**`: Include one route test that authenticates as `tech` and one as `manager` so the generic role boundary stays intentional.
3. `**packages/shared/src/domain/extraction.ts**`: Prefer a discriminated fact schema over a loose `type` plus `value` pair if it keeps invalid combinations impossible at parse time.

## Review Summary

**Grade:** 92.7% (Fully ready)
**Critical Issues:** 0 blocking issues found
**Suggested Improvements:** 3 non-blocking improvements suggested

**Recommendation:** Ready for implementation. Suggested improvements are optional refinements and can be folded into execution without another review.

## Next Steps

1. Execute `IP01.7` locally with `plan-executor`.
2. Run the verification commands listed in the plan.
3. Keep `RSJT-007` local ticket status unchanged until implementation and verification pass.
