# IP01.8 - Approval Staged Change Engine - Implementation Plan Review

**Date:** 2026-05-22
**Plan Reviewed:** `IP01.8-approval-staged-change-engine.md`
**Reviewer:** Local implementation-plan reviewer

## Rubric Scorecard

| Dimension | Score | Notes |
| --- | ---: | --- |
| Non-breaking / Stability | 9.0 | Adds staged approval behavior behind new routes and local DB state. The migration is additive except for renaming the decision actor column, which is safe at this project stage. |
| Correctness | 9.2 | Aligns with the existing `approvals`, `audit_events`, auth, Fastify route, and shared Zod patterns. It correctly keeps external execution out of scope. |
| Simplicity / Elegance | 8.8 | Uses one approval service and one repository with focused lifecycle rules. Payload schemas stay generic until later tickets define exact writeback payloads. |
| Completeness | 9.4 | Covers shared contracts, DB migration, repository behavior, audit events, role enforcement, routes, and tests. |
| Convention Alignment | 9.5 | Preserves local-only project management, generic `manager` and `tech` role vocabulary, Drizzle, Fastify, Zod, and deterministic tests. |

**Readiness Score:** 91.9%

**Reasoning:** The plan is ready to execute. It handles the missing approval metadata in the database before layering service and route behavior on top.

## Critical Issues

None. The plan is ready for implementation.

## Suggested Improvements

1. **`packages/db/src/migrations/meta`**: If Drizzle generation is available locally after schema edits, prefer generating the migration and metadata instead of hand-editing metadata.
2. **`apps/api/src/repositories/approvals-repository.ts`**: Keep audit metadata small and structured, especially for edit operations. Store `previousPayload` only if tests need it, otherwise `approvalId`, `state`, and `reason` are enough.
3. **`apps/api/src/services/approval-service.ts`**: Use explicit domain errors for missing approval, non-pending approval, and insufficient role so route tests are stable.

## Review Summary

**Grade:** 91.9% (Fully ready)
**Critical Issues:** 0 blocking issues found
**Suggested Improvements:** 3 non-blocking improvements suggested

**Recommendation:** Ready for implementation. Suggested improvements can be folded into execution.

## Next Steps

1. Execute `IP01.8` locally with `plan-executor`.
2. Run the verification commands listed in the plan.
3. Mark `RSJT-008` done after implementation and verification pass.
