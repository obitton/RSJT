# IP01.12 - Messaging Webhook Intake - Implementation Plan Review

**Date:** 2026-05-26
**Plan Reviewed:** `.project-management/3-implementation-plans/01-service-referral-ops-agent/IP01.12-messaging-webhook-intake.md`
**Local Ticket:** RSJT-012

## Rubric Scorecard

| Dimension | Score (0-10) | Notes |
| --- | ---: | --- |
| Non-breaking / Stability | 9.1 | Webhook routes are additive and unauthenticated only because Twilio signatures replace app auth at this boundary. No outbound sends are introduced. |
| Correctness | 9.3 | The plan matches Twilio's form-encoded webhook format, uses SDK validation, preserves passthrough params, and accounts for status callback variability. |
| Simplicity / Elegance | 9.0 | The design keeps signature verification, service orchestration, and persistence separated without adding unnecessary messaging abstractions. |
| Completeness | 9.3 | It covers dependencies, schemas, DB migration, idempotency, media metadata, webhook events, route behavior, and tests. |
| Convention Alignment | 9.1 | It follows local-only planning, Fastify route patterns, Zod DTOs, Drizzle schema changes, and fixture-only integration tests. |

**Readiness Score:** 91.8%

**Reasoning:** The plan is ready for implementation. The main operational risk is validating the exact public URL used by Twilio signatures, and the plan addresses that by using `APP_BASE_URL`.

## Critical Issues

None. The plan is ready for implementation.

## Suggested Improvements

1. **`apps/api/src/integrations/twilio/twilio-webhook-verifier.ts`:** Add one test that fails validation when the same params are signed against a different URL. This protects future deploy URL changes.
2. **`apps/api/src/repositories/messages-repository.ts`:** Make duplicate `MessageSid` behavior explicit in tests, because Twilio can retry webhooks.
3. **`packages/shared/src/domain/messaging.ts`:** Keep raw Twilio payload schemas as passthrough objects and map to normalized internal types in the service, not in route code.

## Review Summary

**Grade:** 91.8% (Fully ready)

**Critical Issues:** 0 blocking issues found
**Suggested Improvements:** 3 non-blocking improvements suggested

**Recommendation:** Ready for implementation. Suggested improvements should be included because they reduce webhook edge-case risk.

**Next Steps:**

1. Execute `IP01.12` before customer intake state machine work.
2. Run DB-backed tests with local Postgres before marking RSJT-012 done.
