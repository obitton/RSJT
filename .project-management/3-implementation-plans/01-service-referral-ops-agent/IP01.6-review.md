# IP01.6 - Record Matching Service - Implementation Plan Review

**Date:** 2026-05-19
**Plan Reviewed:** `IP01.6-record-matching-service.md`
**Local Ticket:** RSJT-006

## Rubric Scorecard

| Dimension | Score | Notes |
|-----------|-------|-------|
| Non-breaking / Stability | 9.2 | Matching remains local-state-only, RepairShopr stays read-only, and the refined repository plan now clears stale selected job links safely. |
| Correctness | 9.0 | The shared schemas, DB mapping, repository invariants, route shape, and service boundaries align with the current codebase. |
| Simplicity / Elegance | 8.8 | The service/repository/route split is clean, with deterministic scoring and no new fuzzy-search dependency. |
| Completeness | 9.0 | Covers shared contracts, migration, persistence, scoring, routes, service tests, route tests, and repository-level state tests. |
| Convention Alignment | 9.4 | Preserves Zod, Drizzle, Fastify, fixture-backed testing, local-only project management, and manager/tech vocabulary. |

**Readiness Score:** 91.0% (Fully ready for implementation)

**Reasoning:** The refined plan addresses the prior state-consistency blockers and now provides direct repository tests for the highest-risk local match transitions. Remaining notes are implementation details, not blockers.

## Critical Issues

None. The plan is ready for implementation.

## Suggested Improvements

1. **Contact search coverage:** `IP01.6` searches customers, leads, tickets, appointments, invoices, and payments, while the read client also supports contacts. During implementation, either include `listContacts` in `RepairShoprMatchReader` for phone/email/name searches or explicitly document why contact-only records are deferred.

2. **Repository test setup:** `apps/api/src/repositories/matches-repository.test.ts` should define a deterministic local DB setup and cleanup pattern, such as unique job IDs plus cleanup in `afterEach`, so API tests do not become order-dependent.

3. **Migration application:** Before applying `0001_match_candidate_references.sql`, inspect whether local `match_candidates` rows exist. The plan already notes this, but the executor should choose reset or nullable/backfill/not-null based on actual local data.

## Review Summary

**Grade:** 91.0% (Fully ready)

**Critical Issues:** 0 blocking issues found
**Suggested Improvements:** 3 non-blocking improvements suggested

**Recommendation:** Ready for implementation. Suggested improvements are optional implementation guardrails.

**Next Steps:**

1. Execute `IP01.6`.
2. Run the verification commands listed in the plan before marking `RSJT-006` done.
