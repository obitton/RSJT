# IP01.18 - Closeout Reminders - Implementation Plan Review

**Date:** 2026-05-26
**Implementation Plan:** IP01.18-closeout-reminders.md
**Reviewer:** Codex

## Rubric Scorecard

| Dimension | Score (0-10) | Notes |
| --- | ---: | --- |
| Non-breaking / Stability | 9 | Reuses the existing reminders table, adds new routes and screens, and starts the background runner only outside tests. |
| Correctness | 9 | Derives reminders from current job state, money fields, expense rows, and follow-up facts without requiring external writes. |
| Simplicity / Elegance | 9 | Keeps reminder identity as strict shared reason codes instead of adding a migration for fields that v1 does not need. |
| Completeness | 9 | Covers shared contracts, repository sync, service rules, job runner, API routes, mobile inboxes, route registration, tests, and verification. |
| Convention Alignment | 9 | Matches recent RSJT patterns for Zod contracts, Fastify auth guards, service interfaces, repository tests, mobile validators, and Expo Router groups. |

**Readiness Score: 90%**

**Reasoning:** The plan is ready for implementation. The main tradeoff is using application-level reason codes in the existing text column, which is acceptable because all reads and writes pass through shared strict schemas.

## Critical Issues

None. The plan is ready for implementation.

## Suggested Improvements

1. **Future data model:** If reminders later need due dates, snooze, or assigned users, add a migration instead of overloading the `reason` field.
2. **Follow-up audit trail:** If manual follow-up resolution needs durable evidence, add a local note field or audit event in a later revision.

## Review Summary

**Grade: 90%** (Fully ready)

**Critical Issues:** 0 blocking issues found
**Suggested Improvements:** 2 non-blocking improvements suggested

**Recommendation:** Ready for implementation. Suggested improvements are optional enhancements.

**Next Steps:**

1. Implement shared reminder schemas and tests.
2. Implement the API repository, service, job runner, and routes.
3. Add mobile reminder screens and run full verification.
