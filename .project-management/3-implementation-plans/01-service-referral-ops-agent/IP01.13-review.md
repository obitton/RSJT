# IP01.13 - Customer Intake State Machine And Spam Gate - Implementation Plan Review

**Date:** 2026-05-26
**Plan Reviewed:** `.project-management/3-implementation-plans/01-service-referral-ops-agent/IP01.13-customer-intake-state-machine-spam-gate.md`
**Local Ticket:** RSJT-013

## Rubric Scorecard

| Dimension | Score (0-10) | Notes |
| --- | ---: | --- |
| Non-breaking / Stability | 9.0 | The plan extends conversation state and approval staging without sending replies or writing RepairShopr records. |
| Correctness | 9.1 | It respects existing matching and approval boundaries, handles unavailable live reads honestly, and makes webhook intake the trigger point. |
| Simplicity / Elegance | 8.9 | A current-state snapshot on `conversations` is simpler than a history table for v1. The deterministic extraction rules are intentionally narrow. |
| Completeness | 9.3 | The plan covers state transitions, minimum fields, spam rules, approval staging, route reads, tests, and integration with IP01.12. |
| Convention Alignment | 9.3 | It keeps local-only state, uses `manager` and `tech`, avoids live writes, and fits the existing service/repository/test structure. |

**Readiness Score:** 91.3%

**Reasoning:** The plan is ready for implementation. Its biggest risk is overreaching with customer identity inference, and the plan limits this by staging approvals and leaving unavailable live matching unmatched.

## Critical Issues

None. The plan is ready for implementation.

## Suggested Improvements

1. **Spam gate constants:** Keep the high-risk phrase list small and test-driven. A broad keyword list can block legitimate repair requests.
2. **State transitions:** Put transition tests next to the service rather than only route tests, since most correctness lives below HTTP.
3. **Approval creation:** Prefer one pending prompt approval per conversation and update it instead of creating duplicates after every inbound message.

## Review Summary

**Grade:** 91.3% (Fully ready)

**Critical Issues:** 0 blocking issues found
**Suggested Improvements:** 3 non-blocking improvements suggested

**Recommendation:** Ready for implementation. Suggested improvements can be applied during execution.

**Next Steps:**

1. Execute `IP01.13` after IP01.12 webhook persistence exists.
2. Verify no customer-facing send path is introduced before marking RSJT-013 done.
