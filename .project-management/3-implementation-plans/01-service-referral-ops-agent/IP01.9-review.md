# IP01.9 - Expo Auth Shell - Implementation Plan Review

**Date:** 2026-05-22
**Plan Reviewed:** `.project-management/3-implementation-plans/01-service-referral-ops-agent/IP01.9-expo-auth-shell.md`
**Local Ticket:** RSJT-009

## Rubric Scorecard

| Dimension | Score (0-10) | Notes |
| --- | ---: | --- |
| Non-breaking / Stability | 9.2 | The plan scopes the mobile work to a shell and keeps server behavior unchanged. SDK upgrade risk is addressed by using official Expo install tooling and verification. |
| Correctness | 9.4 | The planned auth routes match the existing Fastify API: `/auth/login`, `/auth/session`, and `/auth/logout`. Role routes avoid duplicate `/` indexes inside Expo Router groups. Mobile uses shared auth DTOs as type-only imports to avoid Metro bundling the NodeNext shared barrel. |
| Simplicity / Elegance | 9.0 | The design uses plain Expo Router, React context, TanStack Query, SecureStore, and a small fetch client without introducing a UI framework or extra auth abstraction. |
| Completeness | 9.3 | File-level changes, route groups, storage behavior, environment config, and verification commands are specified. |
| Convention Alignment | 9.4 | The plan follows the local project-management flow, uses shared Zod contracts, avoids external communication tools, and keeps app roles generic. |

**Readiness Score:** 93.0%

**Reasoning:** The plan is implementation-ready. The main integration risks, Expo SDK upgrade behavior, auth loading, API response validation, and role routing, are called out with concrete implementation rules.

## Critical Issues

None. The plan is ready for implementation.

## Suggested Improvements

1. **`apps/mobile/src/auth/session-validation.ts`:** Keep the local runtime guards limited to the auth shell DTOs. If the mobile app needs broader shared runtime schemas later, add a built shared package output instead of duplicating additional validators.
2. **Expo startup verification:** Prefer the web target for the first local visual check with `pnpm --filter @rsjt/mobile start -- --web --port 47631`. It compiles the route tree quickly and avoids requiring a simulator before the shell is useful.

## Review Summary

**Grade:** 93.0% (Fully ready)

**Critical Issues:** 0 blocking issues found
**Suggested Improvements:** 2 non-blocking improvements suggested

**Recommendation:** Ready for implementation. Suggested improvements should be applied during execution because they are small and reduce auth-shell risk.

**Next Steps:**

1. Execute `IP01.9` with `plan-executor`.
2. Run the mobile and workspace verification commands before marking RSJT-009 done.
