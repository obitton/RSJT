{
  "name": "rsjt",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "db:up": "docker compose up -d postgres || docker start rsjt_postgres || docker run --name rsjt_postgres -d --restart unless-stopped -p 54329:5432 -e POSTGRES_DB=rsjt_dev -e POSTGRES_USER=rsjt -e POSTGRES_PASSWORD=rsjt_local -v rsjt_postgres:/var/lib/postgresql/data postgres:16-alpine",
    "db:down": "docker compose down || docker stop rsjt_postgres",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
